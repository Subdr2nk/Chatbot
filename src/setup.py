from setuptools import setup

setup(
    name="skybot",
    version=0.6,
    author="Caique Santos Carvalho",
    author_email="subdr2nk@gmail.com",
    packages=['skybot'],
    install_requires=['setuptool >= 40.8.0'],
    python_requires=['python >= 3.8.0'],
    zip_safe=False)
